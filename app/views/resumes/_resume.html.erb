    <% if @resume.present? %>

    <%= link_to "#{@current_user.name} Resume", resume_path(id: @resume.id) %><br>
    <iframe src=<%= resume_path(id: @resume.id) %> width="100%" height="800" style="border: none;"></iframe>

<% else %>
    <h2 class="alert">You currently do not have a resume on record. To create one use the form on the left.</h2>
    <% end %>
